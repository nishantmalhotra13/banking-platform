# Profile: local — IntelliJ / CLI dev against Docker infra on localhost

spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/ccsl_db
    username: ccsl_user
    password: ccsl_pass

server:
  port: 8082

logging:
  level:
    com.banking.platform: DEBUG
    org.hibernate.SQL: DEBUG

